{% extends 'layouts/app.twig' %}

{% block title %}User{% endblock %}

{% block content %}
	<p>Customer info:</p>
	{% if not user %}
		<p>No user found.</p>
	{% else %}
		<p>{{ user.username }} - <i>{{ user.email }}</i></p>
	{% endif %}
{% endblock %}
