{% extends 'layouts/app.twig' %}

{% block title %}User{% endblock %}

{% block content %}
	{% if not user %}
		<p>No user found.</p>
	{% else %}
		<h1>{{ user.username }} <i>{{ user.email }}</i></h1>
	{% endif %}
{% endblock %}